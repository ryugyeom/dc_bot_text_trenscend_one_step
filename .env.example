
DISCORD_TOKEN=여기에_봇_토큰

# 테스트 서버 ID (우클릭 → ID 복사). 설정하면 슬래시 커맨드가 즉시 등록됨.
# 비우면 글로벌 등록 (최대 1시간 소요)
GUILD_ID=

# GIF 카운트다운에서 클라이언트 로딩 시간으로 미리 빼는 ms (기본 600).
# GIF 노트가 음악보다 "앞서" 가면 값을 키우고, "늦게" 가면 줄이세요.
GIF_LOAD_EST=600
